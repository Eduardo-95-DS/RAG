# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv
.env
.python-version
groq_available_models.py

# Logs
rag.log
rag.log.*

# Local FAISS index (downloaded from GCS for eval/debugging on rag-gcp,
# or built locally on other branches) — never committed, see
# reference/rag-gcp/known_issues.md ("CPU-only torch wheel install" /
# index-persistence entries) for why a committed index is the wrong pattern.
faiss_index/

# Local-only retrieval eval script. Superseded in CI by ci/retrieval_eval.py
# (run via the rag-gcp-eval-manual Cloud Build trigger — see
# cloudbuild-eval.yaml). This folder stays on disk for local experimentation
# but is no longer tracked in git, so ci/ is the single source of truth for
# what actually gates anything.
eval/