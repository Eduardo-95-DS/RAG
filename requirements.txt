# Streamlit
streamlit==1.48.1

# Environment
python-dotenv==1.1.1

# LangChain
langchain==0.3.27
langchain-community==0.3.27
langchain-core==0.3.86
langchain-groq==0.3.8
langchain-text-splitters==0.3.9

# Vertex AI embeddings (deprecated class, kept — see vectorstore.py comment
# for why: IAM auth consistency + stable text-embedding-005 support).
# Pinned to 2.1.2, not latest (3.x) — 3.x requires langchain-core>=1.3.2,
# which conflicts with langchain==0.3.27/langchain-groq==0.3.8/etc, all
# pinned to the langchain-core 0.3.x line. Verified via pip --dry-run that
# 2.1.2 resolves cleanly against the rest of this file; 3.2.4 does not.
langchain-google-vertexai==2.1.2

# LangGraph
langgraph==0.6.5
langgraph-prebuilt==0.6.4
langgraph-checkpoint==2.1.1

# Vector store
faiss-cpu==1.12.0

# Cross-encoder reranker only (embeddings now use Vertex AI, see above).
# Still needed for CrossEncoderReranker in vectorstore.py.
sentence-transformers==5.5.1
torch==2.12.0
transformers==5.12.1
tokenizers==0.22.2
huggingface-hub==1.19.0
safetensors==0.8.0

# Document loading
pypdf==6.0.0
beautifulsoup4==4.13.4

# Cloud Storage
google-cloud-storage==2.18.0

# Feedback storage
google-cloud-firestore==2.28.0

# Tools
groq==0.37.1

# Pydantic
pydantic==2.11.7
pydantic-settings==2.10.1

# Hybrid search
rank_bm25==0.2.2
